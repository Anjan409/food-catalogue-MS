server:
  port: 9092

spring:
  profile:
    active: dev
  application:
    name: FOOD-CATALOGUE-SERVICE
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
